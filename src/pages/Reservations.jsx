import { Typography } from 'antd';

const { Title } = Typography;

const Reservations = () => {
  return (
    <div>
      <Title level={2}>Reservations Schedule</Title>
    </div>
  );
};

export default Reservations;
